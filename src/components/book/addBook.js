import React, { Fragment } from 'react';
import Sidebar from '../layout/Sidebar';

const addBook = () => {
  return (
    <Fragment>
      <h1>Add Book</h1>
    </Fragment>
  );
};

export default addBook;
